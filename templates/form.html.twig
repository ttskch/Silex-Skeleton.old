{% extends "layout.html.twig" %}

{% block subtitle %}Form Sample{% endblock %}

{% block content %}
    <ul class="nav nav-tabs">
        <li><a href="{{ url('homepage') }}">Home</a></li>
        <li class="active"><a>Form Sample</a></li>
        <li><a href="{{ url('pagination') }}">Pagination Sample</a></li>
    </ul>

    <h3>Form Sample</h3>
    <hr/>

    <div class="row">
        <div class="col-sm-6">
            {{ form(form) }}
        </div>
    </div>
{% endblock %}
